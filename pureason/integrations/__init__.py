"""PureReason verification middleware for common frameworks.

These integrations verify outputs from your own systems using PureReason's
pure Rust ECS engine — no LLM required from PureReason's side.

Available:
- pureason.integrations.langchain  — LangChain callback handler
- pureason.integrations.decorator  — @reasoning_guard function decorator
"""
